# Suprimir avisos conhecidos de vulnerabilidades em dev dependencies
audit-level=moderate

# Ignorar vulnerabilidades em pacotes que não vão para produção
# (Aceternity UI é apenas uma CLI, não é empacotado no bundle final)
